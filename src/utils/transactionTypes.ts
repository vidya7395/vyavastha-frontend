export enum TransactionType {
  Expense = 'expense',
  Income = 'income'
}
//'active', 'paused', 'cancelled'
export enum StatusType {
  Active = 'active',
  Paused = 'paused',
  Cancelled = 'cancelled'
}
